{{- if .Values.global.cluster.istio.enabled }}
{{- if .Capabilities.APIVersions.Has "networking.istio.io/v1beta1" }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "edusharing_elasticsearch.name" . }}
  labels: {{ include "edusharing_elasticsearch.labels" . | nindent 4 }}
spec:
  hosts:
  - {{ include "edusharing_elasticsearch.name" . }}
  http:
  - route:
    - destination:
        host: {{ include "edusharing_elasticsearch.name" . }}
{{- end }}
{{- end }}
