apiVersion: v2

name: edusharing_community_repository_service
description: edusharing_community_repository_service

icon: ${helm.chart.icon}

version: ${helm.chart.version}
appVersion: ${docker.tag}
kubeVersion: "${helm.kube.version}"
