apiVersion: v2

name: edusharing_repository_service
description: edusharing_repository_service

icon: ${helm.chart.icon}

version: ${helm.chart.version}
appVersion: ${docker.tag}
kubeVersion: ">= 1.16.0"
