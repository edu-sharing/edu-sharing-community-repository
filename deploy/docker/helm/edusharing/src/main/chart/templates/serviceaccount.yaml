{{- if .Values.global.cluster.prometheus.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "edusharing.name" . }}-prometheus
  labels: {{ include "edusharing.labels" . | nindent 4 }}
{{- end }}
