{{- if .Values.global.istio.enabled }}
{{- if .Capabilities.APIVersions.Has "networking.istio.io/v1beta1" }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "edusharing_elasticsearch.fullname" . }}
  labels: {{ include "edusharing_elasticsearch.labels" . | nindent 4 }}
spec:
  hosts:
  - {{ include "edusharing_elasticsearch.fullname" . }}
  http:
  - route:
    - destination:
        host: {{ include "edusharing_elasticsearch.fullname" . }}
{{- end }}
{{- end }}
