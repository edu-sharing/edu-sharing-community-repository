services:

  repository-search-solr4:
    image: "${project.parent.artifactId}-build-search:${project.version}"

  repository-service:
    image: "${project.parent.artifactId}-build-service:${project.version}"

  repository-transform:
    image: "${project.parent.artifactId}-build-transform:${project.version}"
