import { Injectable } from '@angular/core';

@Injectable()
export class MdsViewerInstanceService {
    /** The heading level from 1 to 6 to use for widget labels, equivalent to `h1` to `h6`. */
    headingLevel = 2;
}
