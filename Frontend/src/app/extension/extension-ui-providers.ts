// Overwrite this file in extensions to define additional providers (for the ui lib, i.e. I18N_CONFIG).

import { Provider } from '@angular/core';

export const extensionUiProviders: Provider[] = [];
