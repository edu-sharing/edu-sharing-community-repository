{
  "name": "ngx-edu-sharing-rendering-web-component",
  "version": "0.0.1",
  "//": [
    "We use --deploy-url here although Angular complains it is deprecated since this seems to be ",
    "the only way to get Angular to load lazy scripts from a route that differs from base HREF as ",
    "given by index.html (which belongs to the host page and we don't control)."
  ],
  "scripts": {
    "start": "ng serve",
    "preview": "npm run build && npm run serve",
    "build": "ng build rendering-service",
    "publish": "npm run build && cp {package.json,README.md} ../../dist/rendering-service-web-component && cd ../../dist/rendering-service-web-component && npm publish",
    "serve": "source ../../.env && http-server --gzip --port 4200 --proxy $BACKEND_URL example"
  },
  "devDependencies": {
    "@angular/elements": "^18.0.3"
  }
}
