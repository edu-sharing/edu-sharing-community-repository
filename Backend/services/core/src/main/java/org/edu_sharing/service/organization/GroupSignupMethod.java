package org.edu_sharing.service.organization;

public enum GroupSignupMethod {
    simple, // simple signup, no password required
    password, // signup via passwort
    list // signup via waiting list
}
